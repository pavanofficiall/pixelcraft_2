/// <reference types="react" />
import { InputFieldProps } from '../InputField';
export interface FileFieldProps extends InputFieldProps {
    btnLabel?: React.ReactNode;
    accept?: string;
    isContain?: boolean;
    clear?: {
        tooltip?: string;
    };
    onChange?(value: string, partial?: boolean): void;
    onOpen?(value: string | null): void;
}
export default function FileField({ className, value, label, btnLabel, size, isContain, clear, onChange, onOpen, description }: FileFieldProps): import("react/jsx-runtime").JSX.Element;
